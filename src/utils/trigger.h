#pragma once
#include <Arduino.h>
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------
class trigger
{
	public:
		trigger();
		~trigger();
		
		void start(uint32_t duration);
		void stop();
		bool triggered();
		
	private:
	
		bool active;
		uint32_t lastTriggerTime;
		uint32_t duration;
	
};
//-------------------------------------------------------------------------------------------------------------------------------------------------------------------------
